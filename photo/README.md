# Папка `photo`

Тут зберігаються фотографії сторінки.

- `marina.jpg` — головне фото (використовується на `index.html` і `gallery.html`)

Щоб додати нове зображення в галерею: поклади файл сюди та додай тег `<img>` у `pages/gallery.html`.
